# UFC Predictions Frontend

Minimal Next.js frontend for uploading fights and viewing predictions.

## Quick Start

```bash
cd frontend
npm install
npm run dev
```

By default it expects the backend at `http://localhost:8000`.

Visit `http://localhost:3000` in your browser.
